# AI 활용 내역

## 사용 도구 / 모델

- **Claude (대화형 어시스턴트)** — 설계 논의, 구조 리뷰, 개념 설명, 코드 초안
- **Claude Code (Opus 5)** — 요구사항·API 명세 대비 전체 코드 리뷰, 리뷰 지적사항 반영,
  Playwright MCP를 통한 브라우저 동작 검증

## 적용 작업 범위

### 1차 — 구현 단계 (대화형)

- 아키텍처/폴더 구조 설계 (페이지 코로케이션, 관심사 분리)
- 라이브러리 선택·트레이드오프 검토 (React Query, MSW, zustand 등)
- MSW 핸들러 / React Query 무한·가상 스크롤 구현 방향 논의 및 초안

### 2차 — 리뷰·보완 단계 (Claude Code)

`requirement.md` / `openapi.yaml`과 코드를 대조하는 전체 리뷰를 수행하고, 도출된 지적사항을
우선순위(버그 → 품질 → 접근성) 순으로 반영했습니다.

- **버그 수정**
  - 인증 가드가 레이아웃보다 바깥에 있어 GNB 비로그인 분기가 렌더될 수 없던 문제
  - 할 일 삭제 후 대시보드 통계 캐시가 무효화되지 않던 문제
  - 삭제한 할 일의 상세 캐시가 남아 뒤로가기 시 되살아나던 문제
- **접근성** — 카드를 `div[onClick]` → `Link`로 전환(키보드 접근), 모달 `role="dialog"`·
  `aria-modal`·Escape 닫기, 삭제 확인 입력 `aria-label`
- **타입 안정성** — `ErrorResponse` 타입 정의 후 `isAxiosError<ErrorResponse>` 적용
  (`any`가 흐르던 유일한 지점 제거)
- **일관성** — 모듈 경계를 넘는 상대경로를 `@/` 별칭으로 통일, `ProtectedRoute.tsx` 파일명
  컨벤션 정리, 하드코딩된 `#fff`를 `--color-on-primary` 토큰으로 전환
- **명세 준수** — openapi에 정의된 401에 대응하는 응답 인터셉터 추가(만료 시 재발급 후 원 요청
  재시도, 재발급까지 실패한 경우에만 인증 해제 후 로그인 이동), MSW에 만료 토큰 시나리오 추가
- **토큰 재발급 구현** — openapi의 `/api/refresh`를 401 기반 반응형(reactive)으로 연동.
  동시에 여러 요청이 401을 받아도 재발급 Promise를 공유해 한 번만 호출하고(single-flight),
  `_retry` 표식과 `/refresh` 경로 검사로 무한 루프를 차단하며, 재시도 시 원본 요청의
  `Authorization` 헤더를 새 토큰으로 갱신합니다. 명세 해석과 설계 근거는 README의
  ["토큰 재발급(refresh)"](./README.md#토큰-재발급refresh) 참고.
- **프로덕션 빌드 수정** — 프로덕션에서 MSW 워커를 시작하지 않아 모든 API가 404가 되고 로그인조차
  불가능하던 문제를 수정. 이 프로젝트는 백엔드가 MSW뿐이므로 프로덕션 빌드에서도 워커를 시작합니다.

## 핵심 프롬프트 요약

### 구현 단계

- openapi.yaml 스펙에 맞는 MSW 핸들러 구조
- React Query + react-virtual로 무한·가상 스크롤 통합
- 페이지 코로케이션에서 공유 vs 페이지 전용 컴포넌트 기준
- react-hook-form 렌더 격리 구조

### 리뷰 단계

- "버그/엣지케이스, 타입 안정성, 접근성, 일관성, openapi 준수 관점으로 전체 리뷰.
  개선점은 우선순위대로. 단 과제 범위를 유지하고 오버엔지니어링은 지양할 것"
- "docs의 요구사항·API 문서를 기준으로 다시 전체 점검"

> 리뷰 결과를 그대로 수용하지 않고, 판단이 갈리는 지점은 근거를 요구하며 되물었습니다.
> 예를 들어 "로그인 페이지에 GNB/LNB가 왜 필요한가", "모든 API가 인증을 요구하는데
> 비로그인 상태에서 라우트 맵을 노출하는 것이 맞는가", "토큰이 만료되면 어차피 로그아웃되는데
> 401 처리가 왜 필요한가" 등입니다. 이 과정에서 AI의 초기 제안이 뒤집힌 경우도 있었고
> (비로그인 시 라우트 맵을 숨기자는 제안 → 요구사항 문구상 무조건부이므로 철회),
> 반대로 근거를 확인하고 수용한 경우도 있습니다(인증 가드가 라우팅 레이어에서 먼저
> 차단하므로 401이 발생하지 않음을 실제 네트워크 요청으로 확인).

## 사람이 최종 검증한 내용

- AI가 제시한 코드를 검토·이해한 뒤 반영 — **설명 가능한 코드만 커밋**
- 각 기능 브라우저 직접 테스트 (로그인/목록 스크롤/상세 404/삭제 반영 등)
- 타입체크(`tsc --noEmit`) / 빌드(`pnpm build`) 통과 확인
- openapi.yaml 대비 응답·상태코드 대조 검증
- AI의 리뷰 지적사항을 무조건 수용하지 않고, 요구사항 문서 원문과 대조해 타당성을 판단한 뒤
  반영 여부를 결정

## 자의적으로 결정한 사항

요구사항에 명시되지 않아 직접 판단한 항목입니다. (요구사항 "추가적인 결정 사항이 필요한 경우
자의적으로 결정하신 후 코멘트를 남겨주세요"에 해당)

- **로그인 페이지의 레이아웃 범위** — 모든 라우트가 인증을 요구하므로 비로그인 사용자는 사실상
  `/sign-in`에만 머뭅니다. 로그인 페이지를 독립 레이아웃으로 두면 요구사항의 GNB 비로그인 분기가
  화면에 드러나지 않아, 셸을 유지하는 쪽을 택했습니다.
- **할 일 상세 페이지의 구성** — 요구사항에 상세 페이지의 내용 명세가 없어, 삭제 기능을 두되
  오조작 방지를 위해 할 일 ID를 직접 입력해야 삭제 버튼이 활성화되도록 했습니다.
- **토큰 재발급 방식** — 로그인 응답 본문의 `refreshToken`을 저장하지 않고 쿠키 채널만
  사용하도록 했습니다. `/api/refresh`에 `requestBody`가 없고 보안 스킴이 `refreshTokenCookie`인
  점, 그 설명이 "In browser clients"로 한정된 점을 근거로 삼았습니다. 재발급은 401 기반
  반응형(reactive)으로 구현하고 동시 요청은 single-flight로 묶었습니다. 자세한 근거는 README의
  ["토큰 재발급(refresh)"](./README.md#토큰-재발급refresh) 참고.
