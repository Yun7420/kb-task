# AI 활용 내역

## 사용 도구 / 모델

- **Claude (대화형 어시스턴트)** — 설계 논의, 구조 리뷰, 개념 설명, 코드 초안
- **Claude Code (Opus 5)** — 요구사항·API 명세 대비 전체 코드 리뷰, 리뷰 지적사항 반영,
  Playwright MCP를 통한 브라우저 동작 검증

## 적용 작업 범위

### 1차 — 구현 단계 (대화형)

- 아키텍처/폴더 구조 설계
- 라이브러리 선택·트레이드오프 검토 (React Query, MSW, zustand 등)
- MSW 핸들러 / React Query 무한·가상 스크롤 구현 방향 논의 및 초안

### 2차 — 리뷰·보완 단계 (Claude Code)

`requirement.md` / `openapi.yaml`과 코드를 대조하는 전체 리뷰를 수행하고, 도출된 지적사항을
우선순위(버그 → 품질 → 접근성) 순으로 반영했습니다.

- **버그 수정**
  - 인증 가드가 레이아웃보다 바깥에 있어 GNB 비로그인 분기가 렌더될 수 없던 문제
  - 할 일 삭제 후 대시보드 통계 캐시가 무효화되지 않던 문제
  - 삭제한 할 일의 상세 캐시가 남아 뒤로가기 시 되살아나던 문제
  - 무한 스크롤에서 다음 페이지 로드가 실패해도 `hasNextPage`가 true로 남아, 같은 요청을
    무한히 반복하던 문제 (`isFetchNextPageError`로 중단)
- **실패 경로 처리** — 성공했을 때만 확인해서는 보이지 않는 상황을 목에 일부러 만들어 검증했습니다.
  - 삭제가 실패해도 화면에 아무 반응이 없던 문제 → 모달에 서버 오류 메시지 표시
  - 조회가 실패하면 빈 화면만 남던 문제 → 오류 문구 표시
  - 다음 페이지 로드가 실패하면 이미 불러온 목록까지 사라지던 문제
  - 할 일이 하나도 없을 때 안내 문구가 없던 문제
- **접근성**
  - 카드를 `div` 클릭에서 `Link`로 변경 (키보드로 이동 가능)
  - 모달에 `role="dialog"`·`aria-modal` 추가, Escape 키로 닫기
  - 삭제 확인 입력에 `aria-label` 추가
- **타입 안정성** — `ErrorResponse` 타입을 정의해 `isAxiosError<ErrorResponse>`로 적용했습니다.
  `any`가 흐르던 유일한 지점을 없앴습니다.
- **일관성**
  - 모듈 경계를 넘는 상대경로를 `@/` 별칭으로 통일
  - `ProtectedRoute.tsx` 파일명을 컴포넌트 작명 규칙에 맞춤
  - 하드코딩된 `#fff`를 `--color-on-primary` 토큰으로 전환
- **명세 준수** — 스키마 정의를 그대로 반영했습니다.
  - `TaskItem`이 `additionalProperties: false`라, 목록 응답에서 상세 전용 필드인
    `registerDatetime`을 제외했습니다
  - `DeleteTaskResponse.success`가 `const: true`라, `boolean`이 아닌 리터럴 `true`로
    타입을 정의했습니다 (false가 올 수 없으므로 검사 분기도 두지 않음)
- **토큰 재발급 구현** — openapi의 `/api/refresh`를 401 기반 반응형(reactive)으로 연동했습니다.
  - 토큰이 만료되면 재발급 후 원래 요청을 재시도하고, 재발급까지 실패한 경우에만
    인증을 해제하고 로그인 페이지로 보냅니다
  - 여러 요청이 동시에 401을 받아도 재발급은 한 번만 호출합니다 (single-flight).
    대기하던 요청들은 새 토큰을 받으면 함께 재시도됩니다
  - `_retry` 표식과 `/refresh` 경로 검사로 무한 루프를 막습니다
  - 재시도할 때 원래 요청의 `Authorization` 헤더를 새 토큰으로 갱신합니다
  - MSW에 만료 토큰 시나리오를 두어 이 흐름을 직접 확인할 수 있게 했습니다
  - 명세 해석과 설계 근거는 README의 ["토큰 재발급(refresh)"](./README.md#토큰-재발급refresh) 참고
- **프로덕션 빌드 수정** — 프로덕션에서 MSW 워커가 시작되지 않아 모든 API가 404가 되고
  로그인조차 되지 않던 문제를 수정했습니다. 이 프로젝트는 백엔드가 MSW뿐이라
  프로덕션 빌드에서도 워커를 시작합니다.

## 핵심 프롬프트 요약

### 구현 단계

- openapi.yaml 스펙에 맞는 MSW 핸들러 구조
- React Query + react-virtual로 무한·가상 스크롤 통합
- 페이지 코로케이션에서 공유 vs 페이지 전용 컴포넌트 기준
- react-hook-form 렌더 격리 구조

### 리뷰 단계

- "버그/엣지케이스, 타입 안정성, 접근성, 일관성, openapi 준수 관점으로 전체 리뷰.
  개선점은 우선순위대로. 단 과제 범위를 유지하고 오버엔지니어링은 지양할 것"
- "docs의 요구사항·API 문서를 기준으로 다시 전체 점검"

> 리뷰 결과를 그대로 수용하지 않고, 판단이 갈리는 지점은 근거를 요구하며 되물었습니다.
> 예를 들어 "로그인 페이지에 GNB/LNB가 왜 필요한가", "모든 API가 인증을 요구하는데
> 비로그인 상태에서 라우트 맵을 노출하는 것이 맞는가", "토큰이 만료되면 어차피 로그아웃되는데
> 401 처리가 왜 필요한가" 등입니다. 이 과정에서 AI의 초기 제안이 뒤집힌 경우도 있었고
> (비로그인 시 라우트 맵을 숨기자는 제안 → 요구사항 문구상 무조건부이므로 철회),
> 반대로 근거를 확인하고 수용한 경우도 있습니다(인증 가드가 라우팅 레이어에서 먼저
> 차단하므로 401이 발생하지 않음을 실제 네트워크 요청으로 확인).

## 사람이 최종 검증한 내용

- AI가 제시한 코드를 검토·이해한 뒤 반영 — **설명 가능한 코드만 커밋**
- 각 기능 브라우저 직접 테스트 (로그인/목록 스크롤/상세 404/삭제 반영 등)
- 타입체크(`tsc --noEmit`) / 빌드(`pnpm build`) 통과 확인
- openapi.yaml 대비 응답·상태코드 대조 검증
- AI의 리뷰 지적사항을 무조건 수용하지 않고, 요구사항 문서 원문과 대조해 타당성을 판단한 뒤
  반영 여부를 결정
